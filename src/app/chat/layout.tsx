
// This layout overrides the root layout to provide a full-screen experience for the chat page.
export default function ChatLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
